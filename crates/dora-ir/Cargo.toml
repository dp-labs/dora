[package]
name = "dora-ir"
version = { workspace = true }
edition = { workspace = true }

[dependencies]
melior = { version = "0.18.6", features = [
    "ods-dialects",
] }
mlir-sys = "0.2.2"

[dev-dependencies]
insta = "1.39.0"
